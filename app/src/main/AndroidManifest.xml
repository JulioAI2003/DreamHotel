<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.actividad_login_registro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Actividad_Login_Registro">
        <activity android:name=".ResultadoActivity"></activity>
        <activity android:name=".SepararActivity" />
        <activity android:name=".Login" />
        <activity android:name=".Detalle_hotel_Activity" />
        <activity android:name=".Hoteles" />
        <activity android:name=".Registro" />
        <activity
            android:name=".SplashHotel"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>